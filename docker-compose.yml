version: '3'

services:
  active-finance-agent:
    build: .
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
